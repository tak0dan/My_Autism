On development brach there is a problm. So first thing is that attrset selected in any way has to allow user to either apply the closest name of package, skip it or get to the menu with associated packages to that attrset, whowing what attrset selection it was associated to. Second thing is that staging right after the selection slows down process a lot. Staging is something that should happen after user selects to build the system with the confirmed selection. Another thing is that the Stage-Unstage{+-} menu is absolutely not intuitive and it needs both description and change from CLI to TTY, so users can navigate with attows, wasd, or press corresponding to the actions buttons (Y,n,esc, whatever). If there is passed name of the package that was not found (mostly about import) - it should have similar behavior as import function from the main branch, where packages are fetched and closest packages by name are found, shown and user can either select all of them, closest one by the name, manually select in fzf menu or skip. Chant should apply deletions AFTER wanted, so even if one package or attrset was selected as the one to install and then selected as the one to remove - chant will have to eliminate the created module, because if system already has package a installed, it was selected to install and then selected to remove - package a that already exists in the directory will not be removed.